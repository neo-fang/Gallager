name: ${CTRLX_COMPOSE_PROJECT_NAME:-ctrlx}

services:
  relay:
    build:
      context: .
      args:
        CTRLX_VERSION: ${CTRLX_VERSION:?set CTRLX_VERSION in the selected environment file}
        CTRLX_SOURCE_REVISION: ${CTRLX_SOURCE_REVISION:?set CTRLX_SOURCE_REVISION in the selected environment file}
        CTRLX_SOURCE_REPOSITORY: ${CTRLX_SOURCE_REPOSITORY:-https://github.com/jicezeng/CtrlX}
    ports:
      - "${CTRLX_RELAY_HOST_ADDRESS:-127.0.0.1}:${CTRLX_RELAY_PORT:-8080}:8080"
    env_file:
      - ${CTRLX_ENV_FILE:-.env.production}
    environment:
      - LOG_LEVEL=${LOG_LEVEL:-warning}
      - CTRLX_VERSION=${CTRLX_VERSION}
      - CTRLX_SOURCE_REVISION=${CTRLX_SOURCE_REVISION}
      - CTRLX_SOURCE_REPOSITORY=${CTRLX_SOURCE_REPOSITORY:-https://github.com/jicezeng/CtrlX}
      - CTRLX_PROTOCOL_VERSION=${CTRLX_PROTOCOL_VERSION:-3.0}
      - CTRLX_RELAY_BIND_ADDRESS=0.0.0.0
      - CTRLX_RELAY_PORT=8080
      - PAIRING_CODE_EXPIRY_SECONDS=${PAIRING_CODE_EXPIRY_SECONDS:-300}
      # Licensing (hosted-relay monetization) — leave BOTH unset for self-hosting.
      # Setting only one of the two ids fails the boot on purpose.
      # With licensing enabled, a non-integer value in any of these also fails boot.
      - LEMONSQUEEZY_STORE_ID=${LEMONSQUEEZY_STORE_ID:-}
      - LEMONSQUEEZY_PRODUCT_ID=${LEMONSQUEEZY_PRODUCT_ID:-}
      - TRIAL_DAYS=${TRIAL_DAYS:-7}
      - LICENSE_REVALIDATE_HOURS=${LICENSE_REVALIDATE_HOURS:-24}
      - LICENSE_GRACE_DAYS=${LICENSE_GRACE_DAYS:-7}
      - DATA_DIRECTORY=/data
      - METRICS_TOKEN=${METRICS_TOKEN:-}
      # APNs configuration - set these for push notification support
      # Get these values from your Apple Developer account
      - APNS_KEY_PATH=${APNS_KEY_PATH:-/secrets/AuthKey.p8}
      - APNS_KEY_ID=${APNS_KEY_ID:-}
      - APNS_TEAM_ID=${APNS_TEAM_ID:-}
      - APNS_BUNDLE_ID=${APNS_BUNDLE_ID:-}
      # APNs environment: "development" for Xcode builds, "production" for App Store/TestFlight
      - APNS_ENVIRONMENT=${APNS_ENVIRONMENT:-development}
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    volumes:
      - ./data:/data
      # Mount secrets directory for APNs key (read-only)
      # Place your AuthKey_XXXXXXXXXX.p8 file in ./secrets/AuthKey.p8
      - ./secrets:/secrets:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
