// Debug-macOS.xcconfig
// Debug configuration for macOS targets

#include "Shared-macOS.xcconfig"

// Optional, untracked signing overrides for local development builds.
CTRLX_MAC_DEVELOPMENT_TEAM =
#include? "Local-macOS.xcconfig"
