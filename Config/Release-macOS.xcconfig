// Release-macOS.xcconfig
// Release configuration for macOS targets

#include "Shared-macOS.xcconfig"

// Optional, untracked signing and Sparkle release configuration.
#include? "Local-macOS.xcconfig"
